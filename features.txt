FEATURES:
Start Screen:
> Play
    = Requests for Player Name (to be stored for leaderboards)
    = Campaign (Normal, Hard)
    = Endless (Normal, Hard)
> Settings
    = Configurables
        - smoothness (enemy movement)
        - number of enemies (changes settings.json)
        - enemy speed (for non-smooth movement; no. of seconds to move a tile)
        - regen (no. of tiles to add hit point for Regenerators)
        - cham (frequency of change of colors for Chameleons)
        - shooter & tower rate (no. of bullets per second)
        - shooter & tower speed (no. of seconds for the bullet to travel the diagonal)
        - lives (changes settings.json)
        - rounds (to add...)
> Info
    = to add...
> Leaderboard
    = to add...
> Exit

Game Modes:
> Campaign Mode
    = Normal Difficulty: fixed enemy path 
    = Hard Difficulty: different enemy path per round (towers reset per round)
> Endless Mode 
    = Normal Difficulty: fixed enemy path 
    = Hard Difficulty: different enemy path per round (towers reset per round)

CHANGES/ERRORS/NEED TO MODIFY:
Need to Address:
- Fix enemy hit box after going out of tunnel (fixed medyo)
- Fix the paths to accommodate at most 2 tunnels
- Change the logic for configurables* (fixed narin medyo)
- Change the logic for enemy speed**

Missing Features:
- Info Screen
- Leaderboard Screen
- Pause Screen
- Game Over Screen
- One more color for Bullets, Enemies, ...
- Configurable no. of rounds
- More Tower Levels + logic for upgrading + graphics
- Increasing difficulty per round
- Back Button


* medyo weird kasi if babawasan mo ung speed pero bibilis sya... (i based the logic sa instructions)
** if possible, when you change the enemy speed, it will change for smooth and non-smooth movement (if not, we can make two configurables; one for smooth and one for non-smooth)
** ang pangit ng view ko for the name input for leaderboards